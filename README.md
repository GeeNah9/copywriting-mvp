# copywriting-mvp
MVP web app for APP copywriting management with:  - Public browse/search of strings. - Editor/admin CRUD and bulk translation updates. - Admin CSV import with report. - Admin ZIP export (`locales/*.json`, `export.csv`, `meta/export-manifest.json`). - Supabase schema with RLS for anonymous read and role-based write.
